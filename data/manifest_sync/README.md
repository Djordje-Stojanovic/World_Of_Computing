# Manifest Sync Drafts

Manifest sync drafts live here. Start from `data/capture_templates/manifest_sync_template.tsv`.

Use these drafts to review proposed `assets_manifest.tsv` additions before editing the main manifest.

Do not add a final manifest row until the capture log or rendered-page ledger contains the relevant hash and the rights/quote status is explicit.
