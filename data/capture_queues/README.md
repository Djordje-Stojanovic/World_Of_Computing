# Capture Queues

Pass-specific capture queues live here. Start from `data/capture_templates/capture_queue_template.tsv`.

Naming:

- `i0199_product_interface_queue.tsv`
- `i0201_coding_agent_queue.tsv`
- `i0202_benchmark_source_queue.tsv`

Queue rows are planning rows. A planned row is not evidence until a capture log row records a hash or a failure note records the block.
