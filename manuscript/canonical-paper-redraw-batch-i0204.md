# I-0204 Canonical Paper Redraw Batch

This pass executes the first canonical-paper redraw batch without copying source figures. It adds three original SVGs: a Transformer architecture source redraw, a scaling-law evidence-lane redraw, and a compute-optimal balance redraw. It also audits the existing GPT lineage table and RLHF alignment pipeline into the batch so the pass covers the full I-0204 queue item without duplicating already-good artwork.

The new drawings are deliberately conceptual. A-0146 uses S-0002 to separate encoder-decoder source scope from later decoder-only LLM use. A-0147 uses S-0003 with bounded GPT-3/PaLM context to show scaling-law evidence lanes without numeric curves. A-0148 uses S-0015/S-0190 to correct parameter-count intuition without asserting formulas or later-lab behavior.

The rights posture is conservative: no copied paper geometry, no exact plotted values, no direct source excerpts, and no claim that a schematic proves modern implementation equivalence, emergence, product success, solved alignment, or universal scaling law. Page refs and numeric extraction remain required before any final publication caption makes narrower paper-figure claims.
