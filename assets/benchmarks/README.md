# assets/benchmarks

Source types: leaderboards; benchmark tables; yearly landscape tables

Git policy: normalized TSV/SVG trackable; raw screenshots ignored

Required provenance: benchmark; source_url; snapshot_id; metric; split; date; normalized_table; caveats; no_live_rank_note

Next-pass use: I-0286/I-0289 benchmark table generation
