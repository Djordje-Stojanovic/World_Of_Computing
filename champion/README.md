# Champion

This directory holds the current best book state: manuscript snapshot, scorecard, design files, and rendered PDF when available.

Never overwrite the champion without recording the replacement in `scoreboard.tsv` and preserving enough context to recover the prior state.
